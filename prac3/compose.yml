services:
  web-app:
    container_name: web-app
    build:
      context: .
      dockerfile: cmd/api/Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - FLASK_ENV=development